__pycache__/
*.py[cod]
.venv/
.DS_Store
archive/*
archive/
# --- archive/glass-box ---------------------------------------------------
# Run output, regenerated by `python3 -m glassbox.simulate`
archive/glass-box/runs/
# The demo parachute was produced from a real run; a synthetic copy committed
# here would be a file that looks like the demo and isn't.
archive/glass-box/logs/backup-live*.jsonl
